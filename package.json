{
  "scripts": {
    "compileRust": "./build.bash",
    "build": "node ./build.mjs",
    "start": "esbuild src/script.ts --bundle --loader:.wasm=file --loader:.glsl=file --loader:.wgsl=file --loader:.css=css --serve --servedir=public --outfile=public/script.js --asset-names=[name]",
    "format": "prettier --write './**/*.{mjs,ts,md,json,html,js,yml,yaml,css}' && clang-format -i public/{vert,frag}.glsl"
  },
  "devDependencies": {
    "@tsconfig/strictest": "^2.0.3",
    "@types/node": "^20.11.17",
    "@webgpu/types": "^0.1.40",
    "esbuild": "^0.20.2",
    "prettier": "^3.2.4",
    "typescript": "^5.3.3"
  }
}
