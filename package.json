{
  "scripts": {
    "start": "cd esbuild; go run . serve",
    "typeCheck": "tsc --noEmit",
    "format": "prettier --write --prose-wrap=always './**/*.{mjs,ts,md,json,js,yml,yaml,css}' && superhtml fmt public/*.html && clang-format -i public/{vert,frag}.glsl && (cd computer; cargo fmt)"
  },
  "devDependencies": {
    "@tsconfig/strictest": "^2.0.8",
    "@webgpu/types": "^0.1.68",
    "prettier": "^3.7.4",
    "typescript": "^5.9.3"
  }
}
